// -----------------------------
// Game State Constants
// -----------------------------
const GAME_STATE = Object.freeze({
    WAITING: "waiting",
    PICK_IMAGE: "pick_image",
});

module.exports = {
    GAME_STATE,
};
